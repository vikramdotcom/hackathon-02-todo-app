apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: todo-app
  labels:
    app: todo-app
    component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: todo-app
    component: database
